// PART 1: Other array built-in methods
    // Already talked about push(), pop(), shift(), and unshift()
    // Let's start off with an array of students:
const students = ["Atticus", "Scout", "Boo", "Tom"]

    // Using splice()

